{
  "name": "midas-concurrent-forces",
  "version": "1.2.1",
  "description": "MIDAS CIVIL NX plugin — concurrent forces across an element set",
  "private": true,
  "//": "type:commonjs is deliberate and load-bearing. If any PARENT folder's package.json says type:module, Node loads these plain scripts as ESM — module.exports never fires and every require() in the test harness returns an empty namespace. The browser loads them as classic scripts either way.",
  "type": "commonjs",
  "scripts": {
    "mock": "node mock-midas/server.js",
    "test": "node test/run.js",
    "pack": "node ../../assets/scripts/pack.js --source . --out \"dist/Concurrent Forces v1.2.1.zip\""
  }
}
